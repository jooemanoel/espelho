import { PercentualDirective } from './percentual.directive';

describe('PercentualDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentualDirective();
    expect(directive).toBeTruthy();
  });
});
