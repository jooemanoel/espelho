import { MascaraMonetariaDirective } from './mascara-monetaria.directive';

describe('MascaraMonetariaDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraMonetariaDirective();
    expect(directive).toBeTruthy();
  });
});
